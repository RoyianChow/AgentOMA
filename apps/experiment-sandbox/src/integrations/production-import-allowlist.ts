/**
 * G3 is intentionally empty. Adding an entry requires a new approval package;
 * this array is not a feature flag and cannot be populated at runtime.
 */
export const G3_PRODUCTION_IMPORT_ALLOWLIST = [] as const;
