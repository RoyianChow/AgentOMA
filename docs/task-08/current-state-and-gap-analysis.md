# Task 08 — Current state and gap analysis

**Status: DOCUMENTATION ONLY. No Task 08 runtime or production authority.**

## Scope and evidence boundary

This is Workstream A of [Task 08 — Online Fulfilment, Pickup, and Delivery Coordination](../tasks/autonomous-pharmacy/TASK-08-fulfilment-and-delivery.md). It records the maintained checkout on branch `task-08-fulfilment-delivery` at baseline `89f7611203057c2cf4feb192faa2cba86233aea7`. HEAD matched that baseline and the working tree was clean before preparation of these three documents.

Evidence consists of repository source, configuration, migrations, tests, and documentation inspected locally. No database, Docker service, external system, regulatory website, or real patient record was accessed. No tests were executed for this documentation slice. Existing test files establish intended controls, not a fresh passing result or deployment verification. `NOT FOUND` means absent from the inspected maintained checkout; external deployments and unmerged work are `NOT VERIFIED`.

Controlling instructions are [AGENTS.md](../../AGENTS.md), the full Task 08 specification, [project overview](../PROJECT_OVERVIEW.md), [Task 01 safeguards](../task-01/README.md), and [Task 11](../tasks/autonomous-pharmacy/TASK-11-quality-security-release.md). The [standards mapping](ontario-fulfilment-standards-and-policy-mapping.md) is a review instrument, not a legal or professional approval. The [decision register](production-dependency-and-decision-register.md) identifies owners, unresolved questions, and blocking stages.

Four categories are kept separate throughout:

1. **CURRENT IMPLEMENTATION:** code in the production application tree; deployment is not inferred from code presence.
2. **SYNTHETIC/EXPERIMENTAL IMPLEMENTATION:** isolated sandbox code, with no production identity or external-effect authority.
3. **TASK 08 PROPOSED DESIGN:** requirements and future contracts, not implemented behaviour.
4. **PRODUCTION-BLOCKED WORK:** real prescriptions, inventory, adjudication, payment, professional release, pickup, courier, delivery, dispensing, and claims integrations.

## 1. Current implementation

### 1.1 Application, request, and clinical record inventory

| Area | Current evidence and behaviour | Task 08 boundary or gap |
|---|---|---|
| Application and persistence | `package.json`; `src/lib/db/index.ts`; `drizzle.config.ts`: Next.js 16 application, server components/actions/routes, Drizzle with postgres.js and Supabase PostgreSQL. `src/lib/db/schema/index.ts` exports the current domain schemas. | No Task 08 service, route, schema, or migration exists at the baseline. Production connectivity was not exercised. |
| Public intake | `src/app/(intake)/assessment/actions.ts`, `resolvePharmacy`, `createIntakeSession`, `logTriageExit`; `src/lib/p0-c-boundary-schema.ts`, `publicIntakeBoundarySchema`; `src/lib/db/schema/assessments.ts`, `intakeSession`. Bounded symptom/handoff data, self-reported claim count and existing-prescription category, consent time, expiry, and consumption reference. | Zero-PHI public intake is not a prescription request, patient account, upload, or authorization to dispense. Existing-prescription self-report does not prove refill quantity or prescription validity. |
| Self-check and public demonstration | `src/app/(self-check)/check/` and `src/app/(site)/demo/`; `docs/PROJECT_OVERVIEW.md`, public routes and self-check sections. Self-check is non-identifying, transient, and can produce a client-side summary; demo is a guided read-only synthetic presentation. | Neither is a fulfilment portal or authority to introduce PHI. The production-tree demo is not a substitute for the isolated Task 01 environment. |
| Pharmacist workflow | `src/app/(dashboard)/pharmacist/actions.ts`, `getPendingIntakeSessions`, `getIntakeSessionById`, `upsertPatient`, `createAssessment`; `assessment/page.tsx` requires a usable intake; `AssessmentWorkspace.tsx` holds necessary PHI transiently and clears it. | Existing intake worklist and clinical completion are not an online order, pharmacy review queue for uploaded prescriptions, preparation queue, or release workflow. |
| Patient record | `src/lib/db/schema/assessments.ts`, `patient`: pharmacy-scoped name, DOB, health number, gender; unique pharmacy/health-number identity. | No integrated patient-login relationship, verified patient contact/address book, delegate grant, recipient, or fulfilment preference model. The clinical prescription address snapshot is not a verified delivery address. |
| Clinical prescription snapshot | `src/lib/db/schema/assessments.ts`, `assessment`; `src/lib/clinical-record-types.ts`; `createAssessment` in pharmacist actions. Version-2 records contain consent, clinical history/findings/plan, conditional prescription details, server-derived prescriber snapshot, PCP-notification facts, and `patientChoiceInformedAt`. | This is an assessment record created by the clinical workflow, not a standalone uploaded-document, inventory, dispensing, transfer, refill, renewal, or custody record. Task 08 must not reinterpret `rx_issued` as prepared, released, paid, or dispensed. |
| Eligibility evidence | `src/lib/db/schema/billability-evidence.ts`; `src/lib/p0-c-boundary-schema.ts`; pharmacist `createAssessment`: required identity/eligibility, self/family, existing-prescription facts, and side-by-side advisory claim-history evidence. | This minor-ailment completion boundary is not a fulfilment eligibility engine. Checked-in migration `0018` is documented as not live; live state was not queried. |
| Upload, document, OCR, refill, renewal, transfer | No active Task 08 models/actions/routes found in `src/app/`, `src/lib/`, or schema exports. `docs/task-10/AI-RX-06-synthetic-prescription-extraction.md` marks AI-RX-06 **RETIRED**; its production route and runtime were removed. | No active prescription-upload/OCR boundary may be assumed. Do not revive retired AI-RX-06 or treat a copied clinical record as an authorized prescription transfer. |
| Storage | `docs/PROJECT_OVERVIEW.md`, tech stack/storage section, describes future prescription/referral object storage as not implemented; `src/env.ts` has storage configuration declarations. | Configuration declarations are not an upload, malware scanning, encrypted evidence store, OCR pipeline, retention rule, or approved storage account. No such integration was verified. |

The exact public route folder names above are source evidence, not permission to send test requests or collect information. No patient data is reproduced here.

### 1.2 Actors, identity, scope, and patient choice

| Actor or relationship | Evidence in current implementation | Missing Task 08 authority |
|---|---|---|
| Patient / subject | `patient` and `assessment.patientId` in `src/lib/db/schema/assessments.ts`. A clinical subject exists independently of a staff user. | No integrated Task 05 patient principal, audience, session, verified relationship, or revocation contract. |
| Delegate / agent / caregiver / SDM | Assessment consent fields record patient versus SDM, name, and relationship. Governance requester categories include patient, SDM, and legal representative in `src/lib/db/schema/governance.ts`. | Recording a relationship or requester category does not issue an access grant or authorize collection of medication. No production reusable delegate/recipient grant model found. |
| Pharmacist | `src/lib/db/schema/auth.ts`, `userRole`; `src/lib/auth-guard.ts`; pharmacist actions resolve prescriber and orientation facts server-side. | No Task 08 assignment, live registrant-status verification, preparation/check/release permission matrix, release validity, or revocation workflow. Preserve the existing as-of-right prescriber case. |
| Intern / student | Existing staff roles and supervising-pharmacist relationship in auth schema and `resolvePrescriberIdentity` in pharmacist actions. | Existing assessment supervision is not authority for a Task 08 pharmacist-only transition. |
| Technician | Existing staff role; clinical completion logic restricts billable assessment actions. | No approved fulfilment technical-check scope. Do not infer professional release from a role label. |
| Staff / administrator | Existing staff accounts; `pharmacy_admin` has administrative workflows and may act clinically only through the existing clinical checks. | No generic staff or administrator permission to make professional fulfilment decisions. Administrative authority and professional authority must remain distinct. |
| Support / courier / worker | No production support, courier, or fulfilment-worker identity domain found in auth schema or server guards. | Assignment, minimum access, expiry, revocation, emergency access, and audit contracts are absent. A courier is never automatically the patient's agent. |
| Pharmacy / tenant | `src/lib/pharmacy-config.ts`, `getConfiguredPharmacyId`; `src/lib/db/schema/assessments.ts`, singleton check/index; public `resolvePharmacy` ignores legacy caller pharmacy input. | Server-only `PHARMACY_ID` is authoritative. No patient-driven tenant selection is permitted. |
| Session | `src/lib/auth.ts`: Better Auth, Drizzle session persistence, invitation-only email/password, TOTP, rolling/revocable sessions and database rate limiting. `src/lib/auth-guard.ts` repeats session/role/pharmacy checks; `src/proxy.ts` is only an optimistic redirect. | Staff sessions must not be repurposed as patient sessions. Task 08 needs approved independent actor/subject/audience/assignment checks at each future boundary. |
| Patient pharmacy choice | `assessment.patientChoiceInformedAt` documents that the patient was informed; it does not identify a selected destination. Public QR pharmacy input is ignored for tenant selection. | No neutral pharmacy directory, accreditation freshness, reversible choice, ranking/network disclosure, destination selection, or transfer workflow exists. |
| Pharmacy accreditation / registrant status | Pharmacy and staff profile fields exist; `docs/COMPLIANCE.md` identifies operational verification still required. | Current accreditation, Designated Manager acceptance, active registration, and vendor/source verification are **NOT VERIFIED**. Stored profile values are not proof. |

**Conflict C01:** Task 08 requires active, reversible pharmacy choice and multiple synthetic pharmacy fixtures when testing choice. AGENTS.md requires a single server-owned pharmacy; Task 04 is also single-scope. Do not change `PHARMACY_ID`, expose a tenant selector, or silently constrain Task 08 to a mandatory pharmacy. Royian must resolve the design boundary with identity/privacy/professional reviewers before choice behaviour is implemented (T08-D13).

**Conflict C02:** Task 08 anticipates authenticated patient prescription evidence and addresses. Current public intake must collect zero PHI. A separately approved Task 05 boundary is required; public intake must not be broadened (T08-D06, T08-D14).

### 1.3 Inventory, fulfilment, money, communications, and operational surfaces

| Area | What exists | What does not follow from it |
|---|---|---|
| Reference data | `src/lib/db/schema/reference.ts` and `src/config/ailment-reference.ts`: ailment groups, PINs, claim rules, effective fees/tiers, red flags. | This is not a drug/product catalogue, DIN catalogue, lot, available quantity, expiry, recall, quarantine, shortage, substitution, reservation, or stock-disposition system. No Task 08 inventory runtime found. |
| Pickup / delivery / custody | No current request item, pickup plan, recipient grant, courier assignment, package, handoff proof, signature, transport, temperature, failed-delivery, return, or stock-disposition entity found. | No ready-to-release, shipment, handoff, return-to-stock, or delivery outcome can be reported as implemented. |
| Billing / claims | `src/lib/claims/derive-claim-draft.ts`, `deriveClaimDraft`; `claimDraft` in assessment schema; `createAssessment`; `ClaimDraftPanel.tsx`. Reference-driven advisory draft, immutable/superseding evidence and fee/PIN display, explicit no-HNS-submission language. | No authoritative payer adjudication, dispensing price, copayment/deductible calculation, delivery fee/tax schedule, card handling, authorization/capture/refund, reconciliation ledger, or claim submission. Values must never be copied into Task 08 as constants. |
| Interoperability | `src/app/api/fhir/route.ts`, `POST`: disabled `403` response. Preserved handoff-building scaffold is not a callable production integration. Task 09 explicitly owns reviewed read-only export, not dispensing/adjudication finality. | No connected PMS, wholesaler, payer, payment, courier, address, identity-verification, or notification provider was established. No live endpoint/consumer approval inferred from the scaffold. |
| Follow-up | `src/lib/follow-ups.ts`; `src/lib/follow-up-schema.ts`; `followUp` in governance schema: required plans, recorded attempts, reached/not-reached, evaluation/next step and supersession. | Phone/in-person method categories are documentation, not automatic calling or messaging. Notification receipt cannot close the professional duty. |
| Task 07 communications | `docs/task-07/README.md`: **Workstreams A–J design complete; runtime not started**. Contracts cover contact/consent, generic notices, secure messages, outbox/provider/webhook/reconciliation, privacy/audit/retention. | No live patient contact verification, provider, message transport, secure thread, delivery worker, or approved notification wording. Task 07 remains a consumer, not an authority to mutate fulfilment. |
| Labels, receipts, manifests, tracking | Existing clinical/claim print views and governance `export_manifest` support record access. No fulfilment outer label, package manifest, proof receipt, or tracking page found. | A governance export delivery method or clinical PDF is not medication delivery, dispensing labelling, or accepted proof of handoff. |
| Accessibility / user experience | Public self-check and authenticated governance/assessment UI provide existing responsive, labelled, focus/error patterns; see `docs/PROJECT_OVERVIEW.md` and `src/app/(dashboard)/pharmacist/governance/`. | No Task 08 address, signature alternative, agent handoff, pharmacy comparison, courier, or 375px fulfilment evidence exists. Existing CSS/tests are not Task 08 accessibility approval. |

### 1.4 Current states, event sources, guards, and side effects

These are neighbouring-domain states, not the Task 08 state machine. Exact constants and predicates remain owned by the linked source areas.

| Domain | Current states/transitions and authority | Effects and terminal assumptions |
|---|---|---|
| Public triage / intake | `src/config/triage.ts`, tree outcomes: `ailment`, `not_funded`, `unsure`; emergency screening is separate. `createIntakeSession` stores a bounded, expiring handoff. Pending intake is unconsumed and unexpired under `pendingPredicate` in pharmacist actions. | `intake.created` is best-effort audit, not an atomic fulfilment event. `logTriageExit` writes `triage_exit`, not an assessment or claim. Consumed/expired intake cannot start completion. No prescription validity or order state exists. |
| Assessment | `createAssessment` validates session, role, orientation/prescriber, patient identity, eligibility, existing-prescription facts, persisted intake, and seeded references. Outcomes are `rx_issued`, `no_rx_referral`, `no_rx_otc_or_nonpharm`. | Transactional assessment, required billability evidence, eligible draft/follow-up and required audit; conditional intake consumption. A red-flag exit produces zero claims. A completed assessment ending in referral is a different path. No fulfilment side effect. |
| Claim draft | Active versus superseded record; `supersededById` and database exclusion/immutability controls in assessment schema and migrations. | Draft is a handoff only. Supersession preserves history; no submitted, paid, dispensed, or delivered terminal state. |
| Follow-up | Plan versus attempt; `reached` true/false; `nextStep` is `resolved`, `continue`, `refer`, `new_assessment_needed`; superseded records are historical. `recordFollowUpAttempt` and plan supersession use transactions/locks. | Due/overdue is a worklist projection, not a worker. Required audit and retention apply; a not-reached attempt is not completed contact. No message is sent. |
| Governance | `src/lib/governance.ts` and governance schema: access/correction decision and fulfilment of a records request, holds/release, correction overlays, export manifest, restore-drill record, destruction dry-run/execution. | Record-request fulfilment is unrelated to medication fulfilment. Destruction requires governed database checks; no automatic destruction cron. These APIs must not become Task 08 return/disposal operations. |

### 1.5 Reliability, privacy, governance, and infrastructure

| Control | Repository evidence | Reuse limit / gap |
|---|---|---|
| Transactional completion and races | Pharmacist `createAssessment`: transaction, scoped advisory locks, conditional intake consumption, immutable records and required assessment-created audit. `src/lib/db/__tests__/claim-rules.db.test.ts` exercises races and rollback. | A sound pattern, not a generic Task 08 idempotency receipt, inbox, worker, or external-effect guarantee. No reuse of protected claim logic. |
| Follow-up concurrency | `src/lib/follow-ups.ts`; `src/lib/db/__tests__/follow-ups.db.test.ts`: lock, transaction, supersession and rollback coverage. | Do not close follow-up based on shipment, notice, payment, or handoff. |
| Audit | `src/lib/audit.ts`, `writeAuditWith` and failure handling; `audit_log`, `audit_write_failure`; `src/lib/db/__tests__/audit-hardening.db.test.ts`. Append-only storage and app-role restrictions exist. | Some older paths use best-effort audit. Current metadata support is not a closed Task 08 event catalogue. Future required writes need an approved atomic design; audit is protected from this slice. |
| Retention / access / correction / export | `src/lib/governance.ts`; `src/lib/retention.ts`; governance schema: `retention_policy`, `patient_record_retention`, `record_hold`, `export_manifest`, `access_correction_request`, `record_correction`, `restore_drill`, `destruction_run`; governance database tests. | Existing minor-ailment rules and legal holds do not assign retention periods to uploads, addresses, signatures, custody, payments, courier logs or backups. No silent reuse of one clinical horizon for everything. |
| Cache and client privacy | `src/lib/phi-route-security.ts`: private/no-store, referrer and CSP controls; assessment transient-state clearing; zero-PHI intake. | No future fulfilment PHI in URLs/storage/analytics/notification previews/logs. Existing controls are a starting pattern, not proof that every legacy log path is safe to reuse. |
| Logging | Payload-free errors exist at intake and protected boundaries. `src/app/(dashboard)/pharmacist/settings/actions.ts` and `team/actions.ts` still have exception-object logging paths. | This is a reuse risk, not proof of an observed PHI leak. Do not copy those paths into a future evidence/address flow; Task 11 owns broader logging controls. |
| Encryption / keys / operations | TLS in database client; authenticated staff access; documented Supabase Canadian region. `docs/RESTORE_DRILL.md` and `docs/NEXT_STEPS.md` retain recovery work. | Task 08 field encryption, key custody/rotation, support access, break-glass, incident playbooks, vendor/subprocessor flows, backup deletion and recovery execution are **NOT VERIFIED**. No universal PHIPA hosting rule is inferred. |
| Workers / queues / webhooks | No production fulfilment worker, job queue, cron, outbox/inbox, webhook consumer, dead-letter queue or reconciliation engine found in the current app/schema/package scripts. | A pending-intake or follow-up worklist is a query, not a background queue. Synthetic expiry code below must not be described as production scheduling. |

## 2. Existing synthetic/experimental implementation

In this section, paths explicitly labelled **workspace** and short `src/` or `tools/` paths resolve under `apps/experiment-sandbox/`, not the production root. No sandbox source is imported into the production application.

| Component | Evidence | Reusable idea, not transferable authority |
|---|---|---|
| Isolated environment | `apps/experiment-sandbox/package.json`; `src/env/server.ts`, `src/lifecycle/state.ts`, `src/identity/actor.ts`, `src/fixtures/synthetic.ts` under that workspace; `tools/deny-egress.cjs`, `tools/verify-boundary.mjs`; root `src/lib/security-policy/forbidden-imports.ts`. | Server-owned synthetic fixtures, lifecycle expiry/kill controls, environment rejection and import/egress denial. Fixed-clock examples exist. No production credentials, module imports, real identities, or external effects. |
| Task 01 authority | `docs/task-01/decisions/G1-design-approval.md`, explicit exclusions; `docs/task-01/README.md`. | Historical G1 is for sandbox infrastructure, not later capabilities, hosted preview, production imports or a Task 08 database. Exact-candidate independent review remains a separate gate. |
| Local PostgreSQL foundation | `apps/experiment-sandbox/docker-compose.yml`: loopback-only binding; `src/db/client.ts`, `src/db/schema.ts`, `src/db/migrations/0001_task04_foundation.sql`. | Task 04-owned synthetic schema and roles. Booking capacity is not medication inventory. No Docker/database operation is authorized by this Workstream A. |
| Booking / queue | Workspace `src/db/booking-create.ts`, `booking-retrieve.ts`, `booking-confirm.ts`, `booking-expiry-worker.ts`, `pharmacist-queue.ts`, availability/catalog modules; `/book`, `/pharmacist-queue`, catalog API. | Real transaction/lock/idempotency and minimized-response patterns for later design study only. Booking confirmation is administrative, not pharmacist release. Expiry is a callable worker implementation, not evidence of a configured production scheduler. |
| Synthetic states | Workspace `src/db/schema.ts`: bookings `pending_confirmation`, `confirmed`, `cancelled`, `rescheduled`, `expired`; holds `active`, `consumed`, `released`, `expired`; waitlist `active`, `offered`, `promoted`, `cancelled`, `expired`; offers `pending`, `accepted`, `declined`, `expired`, `cancelled`. | Schema states do not prove all command implementations. Create/confirm/expiry exist; cancellation, rescheduling and waitlist command runtime are documented as not implemented. Never map these states directly to stock or custody. |
| Capabilities / delegation | Workspace `src/booking/authorization.ts`, `synthetic-delegation-fixtures.ts`; digest-only credentials and reusable session-bound capability patterns. Fixtures include active, expired, revoked, wrong subject/scope. | Booking action grants are not patient identity, prescription delegation, authorized medication-recipient grants, or professional authority. |
| Transaction / outbox / tests | Workspace `src/db/transaction.ts`, `authoritative-context.ts`, `idempotency.ts`, `outbox.ts`; `src/__tests__/postgres-booking-infrastructure.postgres.test.ts`. Trusted DB time, scoped context, strict response replay, retry classification, same-transaction synthetic audit/outbox. | Outbox is synthetic and `not_dispatched`. It is not Task 07 delivery or Task 08 external exactly-once execution. |
| Deny adapters | Workspace `src/integrations/adapters.ts`, `denyExternalEffect`, `recordSyntheticEvent`; empty `production-import-allowlist.ts`. Named dispensing, claims, payment, courier, storage and other adapters deny external effects. | A named stub is not an installed vendor integration, recipient, prescription or shipment capability. |
| Virtual care | Workspace `src/virtual-care/contracts.ts`, `fixtures.ts`, guards and route implementation; project status Task 06 detail. | Synthetic participant/consent/location/uncertainty examples only. No telehealth transport, live messages, recording, model call, patient identity, or Task 08 authority. |

The [Task 04 v3 renewal](../task-04/decisions/task-04-synthetic-scope-renewal-v3-2026-08-19.md) remains **DRAFT — NOT GRANTED**; the previous runtime approval is expired. Merge presence and historical tests do not authorize execution, extension, Task 08 reuse, or promotion. No Task 04 file is changed here.

## 3. Task 08 proposed design and gaps

Everything in this section is a required future design, not implemented behaviour. AgentOMA is the repository product name; the Task 08 brief uses AgentRx. Product-facing naming remains a coordinator decision, not an implicit rename.

| Gap | Required future contract / safe floor | Existing component to study | Decision register |
|---|---|---|---|
| G01 — request and evidence | Distinguish request, evidence, authoritative prescription and professional result. New/refill/renewal/transfer are review requests only; upload/OCR never validates. | Strict Zod and server completion patterns, not retired AI-RX-06. | T08-D05, D15–D17 |
| G02 — identity and pharmacy choice | Separate actor, subject, delegate, authorized recipient, registrant, staff, support, courier, tenant, session, assignment and revocation. Neutral reversible choice cannot become browser-controlled tenancy. | Staff guards and synthetic capability patterns only. | T08-D06, D13–D14 |
| G03 — orthogonal state and authority | Separate request, prescription review, inventory, preparation, professional, fulfilment mode, financial, custody and reconciliation state; version every protected transition. | Transaction/locking and immutable-history patterns. | T08-D18–D20 |
| G04 — inventory / preparation / release | Pharmacy-owned product/quantity/storage/recall/expiry confirmation; no automatic substitute, preparation decision, professional check or release. READY derives from current release and all guards. | None is an inventory or dispensing authority. | T08-D19–D20 |
| G05 — money | Estimates remain uncertain; adjudication/payment/claim are separate. No fulfilment transition creates, codes, submits or reverses a claim. | Preserve existing claim derivation; Task 09 is handoff-only. | T08-D08, D21–D22 |
| G06 — pickup / delivery / return | Separate recipient from courier; release before custody transfer; pharmacy-accepted proof before receipt; unknown custody fails closed; returns never auto-restock. | No current handoff system; design only. | T08-D23–D27 |
| G07 — reliable integration | Typed local stubs first; authenticated/versioned inbox and outbox, actor/resource-bound idempotency, acknowledgement, uncertain-outcome reconciliation before retry, safe manual escalation. | Synthetic booking infrastructure as a pattern, not imported authority. | T08-D28–D29 |
| G08 — privacy / communications | Task 05 protected reads; Task 07 generic notices only; no PHI or reusable credentials in public surfaces, telemetry, labels beyond approved necessity or vendor metadata. | Existing no-store/authorization and Task 07 design. | T08-D07, D30–D31 |
| G09 — governance / operations | Closed minimized audit catalogue, field-level retention/holds, incident containment and human reportability/stock disposition decisions. | Governed records mechanisms only; no automatic reuse of periods or exports. | T08-D32–D34 |
| G10 — accessibility / evidence | 375px, keyboard, focus, status feedback, 200%/400% reflow, Bangla/long labels, frequent-action 56px targets, map/camera/GPS/biometric-free alternatives, deterministic synthetic tests. | Existing UI/test patterns; no Task 08 evidence yet. | T08-D35–D37 |

The required canonical workflow names are `REQUESTED`, `VERIFICATION`, `CLARIFICATION`, `INVENTORY`, `PREPARATION`, `PHARMACIST_CHECK`, `READY`, `IN_TRANSIT`, `HANDED_OFF`, `FAILED`, `RETURNING`, `RETURNED`, `CANCELLED`, `UNKNOWN` (Task 08, Workstream F). These are **proposed**, not columns or current runtime states. `HANDED_OFF` requires patient/authorized-agent receipt, not courier pickup; `RETURNED` means confirmed pharmacy custody, not saleable stock; `UNKNOWN` cannot advance to a professional, financial, custody or claim effect.

Later work must inventory every source/destination dimension, actor/assignment/subject guard, evidence, current version, idempotency, side effect, audit/event, message, invalid transition and reconciliation consequence. Workstream A does not approve those transitions or invent their durations, thresholds or professional policy.

## 4. Dependency status and evidence freshness

| Dependency | Baseline evidence | Task 08 integration status |
|---|---|---|
| Task 01 | Sandbox controls are merged; `docs/task-01/README.md` and current status record historical/candidate-bound technical PASS plus outstanding independent review. G1 explicitly excludes later capabilities. | **BLOCKED** for Task 08 runtime until exact scope/lifecycle and Task 11 review; code patterns available for documentation. Not a fresh candidate PASS. |
| Task 03 | `TASK-03-command-centre-dashboard.md` describes a command centre, not an implemented Rx document service; maintained checkout is NOT RUN. Status mentions unmerged work, not inspected integration. | **BLOCKED** prescription-evidence integration; actual ownership/contract **NOT VERIFIED**. |
| Task 05 | `TASK-05-patient-portal.md` and status show no integrated patient identity domain. Clinical subjects/staff accounts are not replacements. | **BLOCKED** identity, delegated access, recipients and tracking. |
| Task 07 | `docs/task-07/README.md` and A–J files are present. No runtime/provider/recipient; Task 07 scope and Checkpoint 1 remain blocked. | **BLOCKED** delivery and secure messaging; design is reusable for handoff planning. No Task 08 stub implemented. |
| Task 09 | `TASK-09-interoperability.md`; FHIR POST stays 403; authority is reviewed read-only export, not adjudication/payment. | **BLOCKED** integration; assignment of the actual finance/PMS authority requires clarification. |
| Task 11 | `docs/task-11/current-state-and-gap-analysis.md`; `.github/workflows/ci.yml`; root security-policy modules. Incremental CI/import/environment/dependency controls, not complete release control plane. | **BLOCKED** Task 08 Checkpoint 1, independent evidence/release gate and protected-route review. |

Additional dependencies: Task 02 governs the unapplied live `0018` and protected governance work; Task 04 runtime is expired/ungranted; Task 10A retired the former Rx extraction surface. No permission to change those tasks is granted.

Evidence conflicts to preserve rather than silently resolve:

- **C03 — task ownership:** Task 08 references Task 03 prescription-evidence and Task 09 finance dependencies, but their current specifications describe a command centre and disabled interoperability. Royian must identify the actual domain owners (T08-D05, D08).
- **C04 — status freshness:** `CURRENT-IMPLEMENTATION-STATUS.md` is dated 2026-08-26 at an older SHA and says Task 07 A–I. The baseline contains the later Task 07 A–J package. Historical test counts and Task 01 PASS must not be transferred to this SHA. `docs/EXPERIMENTAL_SANDBOX.md` also contains older invariance status (T08-D03, D12).
- **C05 — legal wording:** `docs/architecture/SYSTEM-DESIGN-REVIEW.md` describes Canadian hosting as required by PHIPA; Task 08 explicitly prohibits that universal claim. Preserve the repository's Canadian-region posture as a project choice, not a new legal interpretation; professional/privacy review is pending (T08-D30).
- **C06 — absent planning source:** Task 08 names `deep-research-report.md`; no such file was found in the inspected repository file inventory. Its contents are **NOT VERIFIED**, not a basis for invented requirements (T08-D11).
- **C07 — existing clinical record versus future authority:** the current app creates an assessment prescription snapshot and minor-ailment claim draft. Task 08 coordination must not create prescriptions, dispensing or financial finality itself. Explicit handoff provenance is required; this is not permission to remove existing clinical functionality (T08-D17–D18).

## 5. Mandatory stop conditions and production-blocked work

These conditions stop the affected implementation, not this authorized documentation-only preparation:

| Trigger already established or unresolved | Stop / safe continuation |
|---|---|
| No Task 08 exact runtime scope/Checkpoint 1; Task 01 promotion review unresolved; expired Task 04 lifecycle | Stop runnable Task 08 work, reuse requiring execution, Docker and evidence promotion. Continue local source analysis and bounded documentation. |
| Patient/subject/delegate/recipient audience and revocation absent | Stop patient-side PHI, recipient management and protected tracking. Continue a proposed authority matrix without new auth code. |
| Choice would violate server-owned pharmacy, or public intake would receive PHI | Stop those implementations and seek explicit lead/professional/privacy resolution. Do not weaken AGENTS.md. |
| Prescription-evidence owner and financial authority unclear | Stop domain handoffs that would assert validity, dispensing, coverage, payment or claim authority. Preserve independent source truth. |
| Accreditation, Internet Sites Policy interpretation/current review status, professional responsibility, inventory truth or release policy unverified | Stop production pharmacy operation and any simulated behaviour that guesses the unresolved policy. Record missing evidence; independent deny-only design may continue. |
| Task 07/Task 11 boundaries unavailable | Do not bypass with email/SMS SDKs, public tracking, staff-session reuse or informal release controls. |

Additional mandatory tripwires from Task 08 remain explicit even though no such effect is performed here: real PHI/prescriptions/addresses/recipients/vendors in fixtures; unapproved migration/auth change; real accounts or credentials; automated authenticity, renewal, transfer, substitution, counselling or release; unknown controlled/high-risk scope; central fill/remote dispensing/unattended pickup/cross-jurisdictional expansion; estimates presented as facts; pickup without identity/counselling/release; courier pickup before release or treated as patient receipt; guessed address/recipient; failed delivery marked received; ignored loss/tamper/temperature exception; automatic restock; uncertain custody not set to unknown; unsafe vendor retries; leakage into prohibited surfaces; inaccessible alternatives; synthetic activation in production; weakening tenant, audit, retention or billing controls. Apply the full Task 08 **Mandatory stop conditions** list, not only the examples here.

All real prescription processing, inventory effects, professional preparation/check/release, payer/payment/claim transactions, pickup release, courier activity, shipment, handoff and external notifications remain **PRODUCTION-BLOCKED**. No provider, contract, PIA, TRA, accreditation, retention policy, professional interpretation or go-live approval is granted by these documents.

## 6. Workstream A completion boundary and next safe step

This slice creates only this inventory, the standards/policy mapping and the production dependency/decision register. It does not complete the remaining Workstream B–O deliverables, implement a state machine, create test fixtures, approve a schema, or establish a synthetic prototype PASS.

Next, Royian should assign the missing source-domain owners, resolve the single-pharmacy/patient-choice and patient-PHI boundary questions, identify professional/privacy reviewers and authorize the next bounded **documentation** slice. Threat/data-flow and professional-responsibility design can then proceed without claiming unavailable integrations. Runnable synthetic work needs its own exact scope and Task 11 Checkpoint 1; production remains separately gated.
